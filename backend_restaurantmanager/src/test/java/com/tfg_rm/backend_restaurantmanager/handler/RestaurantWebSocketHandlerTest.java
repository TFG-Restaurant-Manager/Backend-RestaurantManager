package com.tfg_rm.backend_restaurantmanager.handler;

import org.junit.jupiter.api.Test;
// import org.mockito.Mockito;
// import org.springframework.web.socket.*;

// import com.tfg_rm.backend_restaurantmanager.websocket.handler.RestaurantWebSocketHandler;

// import java.util.Map;

class RestaurantWebSocketHandlerTest {

    @Test
    void shouldBroadcastMessage() throws Exception {

        // RestaurantWebSocketHandler handler = new RestaurantWebSocketHandler();

        // WebSocketSession session = Mockito.mock(WebSocketSession.class);

        // Mockito.when(session.getAttributes()).thenReturn(
        //         Map.of(
        //                 "restaurantId", 5L,
        //                 "role", "CAMARERO"
        //         )
        // );

        // Mockito.when(session.isOpen()).thenReturn(true);

        // handler.afterConnectionEstablished(session);

        // handler.handleTextMessage(session, new TextMessage("hola"));

        // Mockito.verify(session)
        //         .sendMessage(Mockito.any(TextMessage.class));
    }
}
